// import { getFriends } from "../getUserDetail/fetch_data.js";

// export const searchByPhoneNumber = async (phone: string) => {
//   const friends = await getFriends();
//   return friends.findIndex(
//     (a: any) => a.phone.toLowerCase() === phone.toLowerCase() && !a.isDeleted,
//   );
// };
